using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BapelkesWebVrAnc.Protocols.Installation{
    /// <summary>
    /// Class ini berfungsi untuk memberikan Status Tangan VR apakah berada pada Posisi yang diharapkan
    /// Class ini ditaruh pada HandInstallmentPos
    /// </summary>
    /// 
    public class HandInstallmentStatus : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}

